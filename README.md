A simple blog template for federico-arias.github.io
---------------------------------------------------

This is a blog design with minimal dependencies. Add node packages with 

```bash
npm install
```

Compile css with:

```bash
npm run styles
```

And run with `hugo server`.
